# Local development environment variables for ReliefNet server
# Copy to .env and adjust values as needed. This file is NOT used in production.

# General
NODE_ENV=development
PORT=5000
APP_BASE_URL=http://localhost:5000
# Override host binding for physical device testing on LAN (default is localhost)
# Set to 0.0.0.0 to allow connections from other devices on your network
# HOST=0.0.0.0

# MongoDB (use your dev/Atlas connection string)
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority

# Auth
JWT_SECRET=change_me_for_dev

# Firebase Admin (choose ONE of the two options below)
# Option A: Put serviceAccountKey.json in server/ (recommended for local), leave these empty
FIREBASE_SERVICE_ACCOUNT_BASE64=
FIREBASE_PROJECT_ID=
FIREBASE_PRIVATE_KEY=
FIREBASE_CLIENT_EMAIL=

# SendGrid (optional in dev)
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=sentrive.ai@gmail.com
SENDGRID_REPLY_TO=support@reliefnet.app

# Frontend (optional CORS allow-list)
FRONTEND_URL=http://localhost:5173
